[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "beat-finder-backend"
version = "0.1.0"
description = "Backend API for Beat Finder by UNDRSTND"
requires-python = ">=3.10"

